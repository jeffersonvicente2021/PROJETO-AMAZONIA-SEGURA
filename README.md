# Projeto Amazônia Segura — MVP

Pipeline local de visão computacional (YOLOv8 + OpenCV + SQLite) para detecção/eventos com câmeras IP.

## Como rodar

1. Crie um arquivo `.env` na raiz (use o `env.example` como modelo).
2. Ative a venv e instale deps:
   ```bash
   source .venv/Scripts/activate    # Windows Git Bash
   pip install -r requirements.txt
